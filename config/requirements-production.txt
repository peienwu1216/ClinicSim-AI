# Production environment dependencies (Lemonade Server)
# Excludes local development tools, focused on production runtime

# Base dependencies
-r requirements-base.txt

# AI/LLM related (Lemonade environment)
# Note: Lemonade may have its own AI services, adjustments may be needed
sentence-transformers

# RAG system (production optimized version)
faiss-cpu==1.12.0
langchain==0.3.27
langchain-community==0.3.29
langchain-core==0.3.76
langchain-text-splitters==0.3.11

# Production environment optimization
gunicorn
waitress

# Monitoring and logging
loguru
prometheus-client

# Security related
cryptography
