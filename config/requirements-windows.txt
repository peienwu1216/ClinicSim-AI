# Windows-specific dependencies
# Resolves Windows-specific compatibility issues

# Base dependencies
-r requirements-dev.txt

# Windows-specific dependencies
pywin32; sys_platform == "win32"
winshell; sys_platform == "win32"

# FAISS alternative for Windows (if faiss-cpu has issues)
# faiss-cpu may have compatibility issues on Windows
# Consider using alternative vector database
chromadb
